<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.Watchr.Stopped" parent="AppTheme">
        <item name="colorPrimary">@color/running_green</item>
        <item name="colorPrimaryDark">@color/running_green_dark</item>
    </style>

    <style name="Theme.Watchr.Running" parent="AppTheme">
        <item name="colorPrimary">@color/stop_red</item>
        <item name="colorPrimaryDark">@color/stop_red_dark</item>
    </style>
</resources>
